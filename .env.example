# Configurações de ambiente para produção

# URL da API - Para Netlify Functions
NEXT_PUBLIC_API_URL=https://seu-site.netlify.app/.netlify/functions

# Configuração do banco de dados PostgreSQL (Supabase)
# Recomendado para Render: usar o Session Pooler (porta 6544) com SSL
# NUNCA use aspas ao definir variáveis de ambiente.
# Exemplo:
# DATABASE_URL=postgresql://postgres:<SUA_SENHA>@<host-pooler>:6544/postgres?sslmode=require

# Opcional: Transaction Pooler (porta 6543) — útil para funções/serverless
# DATABASE_URL=postgresql://postgres:<SUA_SENHA>@<host-pooler>:6543/postgres?sslmode=require

# JWT Secret para autenticação
JWT_SECRET=seu_jwt_secret_muito_seguro_aqui

# Configurações opcionais
NODE_ENV=production

# Supabase (Backend - Service Role)
# URL do projeto Supabase e chave de serviço para bypass de RLS no backend
# Em produção (Render), defina estes valores nas Variáveis de Ambiente do serviço backend
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY