export async function generateStaticParams() {
  return [];
}

export default function MonthlyReportPage({ params }: { params: { cellId: string } }) {
  // Esta página pode ser usada para exibir relatórios mensais da célula
  // Por enquanto, retornamos null
  return null;
}