# Configurações para produção
# Substitua pela string de conexão real do Neon Database
DATABASE_URL=postgresql://username:password@ep-xxx-xxx.us-east-1.aws.neon.tech/database_name?sslmode=require

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-for-production
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=5000
NODE_ENV=production

# CORS Configuration - Substitua pela URL real do frontend no Netlify após o deploy
CORS_ORIGIN=https://seu-frontend.netlify.app